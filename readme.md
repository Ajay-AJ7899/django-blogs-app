To run the project

git clone https://github.com/Ajay-AJ7899/django-blogs-app.git

pip install pipenv //if pipenv is not installed before
pipenv install  // install Dependecies
pipenv shell    // activate the virtual environment 
python manage,py runserver
